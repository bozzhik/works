---
interface Props {
  content: string
  classes: string
  transition?: string
}

const {content, classes, transition} = Astro.props
---

<p transition:name={transition} class="text-2xl xl:text-xl lg:text-sm lg:leading-[1.15] uppercase font-normal font-monospaced" class:list={classes} set:html={content} />
